# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
dist-ssr/
build/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local brainstorm artifacts (mockups + companion server state)
.superpowers/

# Background music — too large for GitHub; keep locally in /audio (see audio/README.md)
audio/*.wav
audio/*.mp3

# Raw asset archive caches (processed assets live in apps/client/public/assets)
assets/raw/

# Paid / licensed third-party asset packs — do NOT commit (esp. to a public repo)
itch.io packs/
tools/extracted/

# Python virtualenv used by the asset pipeline (tools/*.py)
.venv/
venv/
__pycache__/
